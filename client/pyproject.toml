[project]
name = "node"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "grpcio-tools>=1.67.1",
    "grpcio>=1.67.1",
    "textual>=0.86.1",
]

[dependency-groups]
dev = [
    "textual-dev>=1.6.1",
]
